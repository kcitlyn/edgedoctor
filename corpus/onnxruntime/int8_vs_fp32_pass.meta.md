# int8_vs_fp32_pass.log
- command:  polygraphy run artifacts/_divergence_work/resnet18_int8.onnx --onnxrt --load-inputs ... --load-outputs ... --atol 6.0 --rtol 300
- machine:  arm64 · Darwin 25.5.0 · CPUExecutionProvider
- versions: polygraphy 0.50.3, onnxruntime 1.27.0, ONNX opset 17
- outcome:  succeeded
- root cause (human-verified): Same model, same inputs, same measured divergence as int8_vs_fp32_fail.log — only the tolerance changed. The metric lines in the two logs are byte-identical; only the verdict lines differ. This is the regression case proving edgedoctor keys on verdicts, not on the presence of error metrics.
- fix that worked: n/a (demonstrates that tolerance is a policy choice, not a measurement)
- generated by: scripts/make_divergence_corpus.py (reproducible)
