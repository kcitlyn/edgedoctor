# ort_many_fallback.log
- command:  InferenceSession(many_fallback.onnx, providers=[CoreMLExecutionProvider, CPUExecutionProvider]) with log_severity_level=0
- machine:  arm64 · Darwin 25.5.0
- versions: onnxruntime 1.27.0, ONNX opset 17
- available EPs: CoreMLExecutionProvider, AzureExecutionProvider, CPUExecutionProvider
- outcome:  succeeded-with-warnings
- root cause (human-verified): Five DIFFERENT ops (Erf, Round, Sign, Ceil, Floor) are unsupported by the
  CoreMLExecutionProvider EP, and each sits between two supported
  convolutions. The accelerator claims only half the graph and the rest is
  cut into five separate partitions — the signature of a systematic coverage
  gap rather than one exotic layer, which is a different diagnosis with
  different advice.
- fix that worked: Check the provider's supported-operator list; consider a different opset or provider rather than removing five ops individually.
- generated by: scripts/make_ort_corpus.py (reproducible)
