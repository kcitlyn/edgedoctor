# ort_partial_fallback.log
- command:  InferenceSession(partial_fallback.onnx, providers=[CoreMLExecutionProvider, CPUExecutionProvider]) with log_severity_level=0
- machine:  arm64 · Darwin 25.5.0
- versions: onnxruntime 1.27.0, ONNX opset 17
- available EPs: CoreMLExecutionProvider, AzureExecutionProvider, CPUExecutionProvider
- outcome:  succeeded-with-warnings
- root cause (human-verified): Erf and Round are not supported by the CoreMLExecutionProvider EP, and they
  sit in the middle of the graph. ORT therefore cut the graph into multiple
  partitions, running the Conv/Relu sections on the accelerator and the
  unsupported ops on CPU. The session SUCCEEDS, so nothing alerts the user —
  but every partition boundary is a synchronization point, and the cost is
  dominated by the number of boundaries rather than the number of CPU nodes.
- fix that worked: Replace or remove the unsupported ops, or accept the split knowingly. Check partition count, not just node count.
- generated by: scripts/make_ort_corpus.py (reproducible)
