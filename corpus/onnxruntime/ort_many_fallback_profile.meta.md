# ort_many_fallback_profile.json
- command:  InferenceSession(many_fallback.onnx, providers=[CoreMLExecutionProvider, CPUExecutionProvider]) with enable_profiling=True, then 5 runs
- machine:  arm64 · Darwin 25.5.0
- versions: onnxruntime 1.27.0, ONNX opset 17
- available EPs: CoreMLExecutionProvider, AzureExecutionProvider, CPUExecutionProvider
- outcome:  succeeded-with-warnings
- root cause (human-verified): The measured counterpart to ort_many_fallback.log. Half the graph runs on
  CPU, so a material share of kernel time lands there — which is what turns
  a placement warning into a quantified cost. Note the trace attributes
  kernel time only; the synchronization at each of the five partition
  boundaries is largely invisible here, so the real cost of the split is at
  least this much.
- fix that worked: Remove or replace the unsupported ops to rejoin the partitions.
- generated by: scripts/make_ort_corpus.py (reproducible)
