# ort_missing_provider.log
- command:  InferenceSession(resnet18.onnx, providers=[TensorrtExecutionProvider, CPUExecutionProvider]) with log_severity_level=0
- machine:  arm64 · Darwin 25.5.0
- versions: onnxruntime 1.27.0, ONNX opset 17
- available EPs: CoreMLExecutionProvider, AzureExecutionProvider, CPUExecutionProvider
- outcome:  succeeded-with-warnings
- root cause (human-verified): TensorrtExecutionProvider is not present in this onnxruntime build (a
  CPU/CoreML wheel on macOS). ORT warns and drops it rather than failing, so
  the session silently runs entirely on CPU. This is the most misleading
  case in the set: the code looks like it requested acceleration and the run
  reports success.
- fix that worked: Install the matching onnxruntime-gpu build, or assert on session.get_providers() after construction.
- generated by: scripts/make_ort_corpus.py (reproducible)
