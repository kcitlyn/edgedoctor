# ort_missing_provider_partial.log
- command:  InferenceSession(resnet18.onnx, providers=[TensorrtExecutionProvider, CoreMLExecutionProvider, CPUExecutionProvider]) with log_severity_level=0
- machine:  arm64 · Darwin 25.5.0
- versions: onnxruntime 1.27.0, ONNX opset 17
- available EPs: CoreMLExecutionProvider, AzureExecutionProvider, CPUExecutionProvider
- outcome:  succeeded-with-warnings
- root cause (human-verified): TensorrtExecutionProvider is unavailable in this build and is dropped with a
  warning, but CoreMLExecutionProvider is present and claims the graph — so
  the run is NOT silently degraded to CPU. Contrast with
  ort_missing_provider.log, which carries the SAME unavailable-provider
  warning but really did fall all the way back. Reporting this log as 'ran
  entirely on CPU' would be a false accusation, so it must produce no
  ED0302.
- fix that worked: Drop the unavailable provider from the list to silence the warning; no performance action is needed.
- generated by: scripts/make_ort_corpus.py (reproducible)
